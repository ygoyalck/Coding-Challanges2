// arr - input array
// n - size of array

int FindSortedArrayRotation(int arr[], int size) {
   for(int i=1;i<size;i++)
   {
     if(arr[i]<arr[i-1])
     {
       return i;
     }
   }
  return -1;
}
